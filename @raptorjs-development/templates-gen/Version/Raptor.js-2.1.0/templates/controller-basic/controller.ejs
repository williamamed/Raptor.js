'use strict';

/**
 * Raptor.js - v2
 * Generado automaticamente
 * 
 * @Route("<%-baseRoute%>")
 * @Controller
 */
class <%-className%>{

	configure(){
		
	}
	<% if(templateName){ %>

	/**
	 * @Get("/render")
	 */
	indexAction(req, res, next){
		res.render("<%-templateName%>");
	}
	<%}%>

	/**
	 * @Get
	 */
	list(req, res, next){
		res.json([]);
	}
	
	/**
	 * @Post
	 */
	add(req, res, next){
		res.show("OK");
	}
	
	/**
	 * @Put("/:id")
	 */
	edit(req, res, next){
		res.show("OK edit "+req.param.id);
	}
	
	/**
	 * @Delete("/:id")
	 */
	delete(req, res, next){
		res.show("OK delete "+req.param.id);
	}

	
}

module.exports=<%-className%>;